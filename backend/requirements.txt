# ===========================================================================
# NL Consulting — AuditorIA Backend
# requirements.txt — dependências de produção com versões fixadas
# ===========================================================================

# ASGI framework
fastapi==0.136.0
starlette==1.0.0

# ASGI server — desenvolvimento e worker de produção
uvicorn[standard]==0.44.0

# Servidor HTTP de produção (gerencia workers uvicorn)
gunicorn==23.0.0

# Validação e serialização de dados
pydantic==2.13.1
pydantic-settings==2.9.1   # leitura de config via env vars

# HTTP assíncrono (usado internamente pelo SDK da OpenAI)
httpx==0.28.1
anyio==4.13.0

# SDK OpenAI (Structured Outputs + AsyncOpenAI)
openai==2.32.0

# Resiliência: retry com exponential backoff
tenacity==9.1.4

# Variáveis de ambiente
python-dotenv==1.2.2

# Upload de arquivos via multipart/form-data
python-multipart==0.0.26
